version: '2'
services:
  docker-fluxday-db:
    container_name: fluxday-db
    image: mysql:5.6
    env_file:
      - db.env

  docker-fluxday-app:
    container_name: fluxday-app
    tty: true
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile.development
    volumes:
      - .:/share
    env_file:
      - app.env
    depends_on:
      - fluxday-db
    links:
      - fluxday-db
    ports:
      - 3000:3000
